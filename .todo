✅ Create the repository
✅ Create subpackages
✅ Get xmake to build a hello-world project
✅ Get xmake to work with Conan packages
✅ Get xmake to build & use subprojects
✅ Record documentation about how I got the project working
✅ Get a good word list
✅ Create a fetch subpackage
  ✅ Make it actually fetch a URL
  ✅ Give it a .text() method
  ❌ Give it a .json() method
✅ Create an ecma262-date subpackage
  ❌ Give it a .toLocaleString() method
  ❌ Give it a .toLocaleTimeString() method
  ❌ Give it a .toLocaleDateString() method
  ✅ Give it a .toISOString() method
❌ Create an html-sessionstorage subpackage for env var access
  ❌ Give it a .getItem() method
❌ Create an html-location subpackage for argv access
❌ Create a url subpackage for URL parsing and processing
  ❌ Create a URLSearchParams class
  ❌ Create a URL class
❌ Create an html-base64 subpackage for atob() and btoa()
❌ Document how the tools/gen-repo-index.sh works
❌ Add VS Code settings.json config for C++ formatting
